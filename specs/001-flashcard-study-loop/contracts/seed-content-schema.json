{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Seed Deck (content-pipeline output)",
  "description": "Contract between content-pipeline/ingest-oxford.ts and the app's first-run seed step. One file per deck under src/content/seed/.",
  "type": "object",
  "required": ["deckId", "name", "sourceLevel", "source", "extractedAt", "cards"],
  "properties": {
    "deckId": { "type": "string", "description": "Stable id, e.g. 'oxford-3000-a1-a2'" },
    "name": { "type": "string", "description": "Human-readable deck name" },
    "sourceLevel": { "type": "string", "enum": ["A1", "A2", "B1", "B2", "C1"] },
    "source": {
      "type": "string",
      "description": "Traceable source identifier recorded in content-pipeline/SOURCES.md (Constitution Principle IV)"
    },
    "extractedAt": {
      "type": "string",
      "format": "date",
      "description": "ISO date the pipeline generated this file"
    },
    "cards": {
      "type": "array",
      "minItems": 1,
      "items": {
        "type": "object",
        "required": ["front", "back", "sourceRef"],
        "properties": {
          "front": { "type": "string", "minLength": 1, "description": "The word" },
          "back": { "type": "string", "minLength": 1, "description": "Definition/example/translation" },
          "sourceRef": { "type": "string", "description": "Pointer back to the exact source entry, for traceability" }
        }
      }
    }
  }
}
